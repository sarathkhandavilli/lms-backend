spring.application.name=lms
server.port=${SERVER_PORT:8080}

# PostgreSQL Database Configuration for Production
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.driver-class-name=org.postgresql.Driver

# Disable SQL init temporarily to test database connection
spring.sql.init.mode=never


# HikariCP specific settings
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.connection-timeout=30000 
 # 30 seconds
spring.datasource.hikari.idle-timeout=600000   
    # 10 minutes
spring.datasource.hikari.max-lifetime=1800000  
    # 30 minutes


# Supabase S3 configuration for production
supabase.s3.endpoint=${SUPABASE_S3_ENDPOINT:https://fazeeviobowkpdejbgui.storage.supabase.co/storage/v1/s3}
supabase.s3.region=${SUPABASE_S3_REGION:ap-south-1}
supabase.s3.access-key=${SUPABASE_S3_ACCESS_KEY:f0dc9cea9f0668dd0b20110c3b36a006}
supabase.s3.secret-key=${SUPABASE_S3_SECRET_KEY:199efaa5f903b196c3188aa8b24208042b401f166d1850b1ba93c33e4e003660}
supabase.s3.bucket=${SUPABASE_S3_BUCKET:images}

# Logging configuration for production
logging.level.com.lms=INFO
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.springframework.boot.sql.init=DEBUG
logging.level.org.springframework.boot.autoconfigure=DEBUG
# Mail configuration for production
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#redis
spring.cache.type=redis
spring.data.redis.url=${REDIS_URL}
spring.data.redis.ssl.enabled=true