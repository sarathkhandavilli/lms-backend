spring.application.name=lms
server.port=8080

# PostgreSQL Database Configuration
#supabase configuration
spring.datasource.url=${SPRING_DATASOURCE_URL}
# spring.datasource.username=postgres.fazeeviobowkpdejbgui
# spring.datasource.password=sarath@123
spring.datasource.driver-class-name=org.postgresql.Driver

#local configuration
# spring.datasource.url=jdbc:postgresql://localhost:5432/lms
# spring.datasource.username=sarath
# spring.datasource.password=sarath@123
# creates tables and inserts data
# spring.sql.init.mode=always

#redis
spring.cache.type=redis
spring.data.redis.url=${REDIS_URL}
spring.data.redis.ssl.enabled=true

# HikariCP specific settings
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.connection-timeout=30000 
 # 30 seconds
spring.datasource.hikari.idle-timeout=600000   
    # 10 minutes
spring.datasource.hikari.max-lifetime=1800000  
    # 30 minutes


#email
spring.mail.host: smtp.gmail.com
spring.mail.port: 587
spring.mail.username:${SPRING_MAIL_USERNAME}
spring.mail.password:${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth: true
spring.mail.properties.mail.smtp.starttls.enable: true


# Path to save images
# Supabase S3 configuration
supabase.s3.endpoint=${SUPABASE_S3_ENDPOINT}
supabase.s3.region=${SUPABASE_S3_REGION}
supabase.s3.access-key=${SUPABASE_S3_ACCESSKEY}
supabase.s3.secret-key=${SUPABASE_S3_SECRETKEY}
supabase.s3.bucket=${SUPABASE_S3_BUCKET}

#local image path
# image_path=D:/spring/images

